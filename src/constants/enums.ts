export enum Commands {
    Delete = "delete",
    Print = "print",
    Iterations = "iterations",
    Generate = "generate",
    Select = "select",
    Add = "add",
    List = "list",
    Config = "config",
}
